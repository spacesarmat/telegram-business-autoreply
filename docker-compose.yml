services:
  telegram-autoreply:
    build: .
    container_name: telegram-business-autoreply
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    environment:
      - TZ=Europe/Bucharest
      - STATUS_PORT=8080
    ports:
      - "18080:8080"
