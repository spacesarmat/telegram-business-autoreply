# v1.5.0 — расширенная мини-CRM

Главное обновление v1.5.0 — карточка заявки стала полноценнее и удобнее для ежедневной работы.

## Добавлено

- 💵 Полная стоимость заявки.
- 💳 Предоплата / уже полученная сумма.
- 🧾 Автоматический расчёт остатка.
- 🗒 Внутренняя заметка администратора, которую клиент не видит.
- 🔎 Поиск по номеру заявки, имени, username, телефону, форме и содержимому заявки.
- 👤 История клиента с предыдущими заявками.
- 🕘 Журнал последних изменений заявки.
- 💬 Редактируемые шаблоны уведомлений для каждого статуса.

## Переменные шаблонов

В `/admin → 💬 Шаблоны статусов` доступны:

```text
{id}
{form}
{status}
{client}
{amount}
{prepayment}
{balance}
```

Например, сообщение статуса «Оплачена» может автоматически включать стоимость, полученную предоплату и остаток.

## Совместимость

SQLite мигрирует автоматически. Старые заявки, формы, бронирования, кнопки и настройки сохраняются.

## Обновление

```bash
sudo docker pull ghcr.io/spacesarmat/tgautoreply:latest
```

После загрузки нового образа пересоздайте контейнер в ZimaOS/CasaOS.
